# ===========================================================================
# ElasticDocker Configuration Template
# ===========================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Generate strong passwords for all credentials
# 3. Update all CHANGE_ME values with real secrets
# 4. NEVER commit .env to Git!
#
# Password Generation Examples:
#   openssl rand -base64 32
#   pwgen -s 32 1
# ===========================================================================

COMPOSE_PROJECT_NAME=elastic

# Elastic Stack Version
# Current stable: 8.19.10 (Latest 8.x - December 2025)
# Alternative: 9.2.4 (Latest 9.x - Requires migration testing)
# See UPGRADE_GUIDE.md for migration instructions
ELK_VERSION=8.19.10

#----------- Resources --------------------------#
# Heap Size Recommendations:
# - Development: ES=2GB, Logstash=1GB
# - Staging: ES=4GB, Logstash=2GB
# - Production: ES=8-16GB, Logstash=4GB
# Rule: Never exceed 50% of total RAM, never > 32GB for ES

ELASTICSEARCH_HEAP=2048m
LOGSTASH_HEAP=1024m

#----------- Hosts and Ports --------------------#
# Internal Docker hostnames - usually no need to change
ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200

KIBANA_HOST=kibana
KIBANA_PORT=5601

LOGSTASH_HOST=logstash

APMSERVER_HOST=apm-server
APMSERVER_PORT=8200

#----------- Credentials ------------------------#
# ⚠️ CRITICAL: Change all default passwords before deployment!
# ⚠️ Use strong passwords: min 20 chars, alphanumeric + symbols
# ⚠️ Never use these defaults in production!

# Elasticsearch superuser credentials
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE_MIN_20_CHARS

# APM Server secret token (used for agent authentication)
# Generate: openssl rand -hex 32
ELASTIC_APM_SECRET_TOKEN=CHANGE_ME_SECRET_TOKEN_HERE

#----------- Cluster ----------------------------#
# Cluster configuration for single-node setup
# For multi-node, see docker-compose.nodes.yml

ELASTIC_CLUSTER_NAME=elastdocker-cluster
ELASTIC_INIT_MASTER_NODE=elastdocker-node-0
ELASTIC_NODE_NAME=elastdocker-node-0
ELASTIC_DISCOVERY_SEEDS=elasticsearch

#----------- For Multinode Cluster --------------#
# Only needed if using docker-compose.nodes.yml
# Uncomment and configure if deploying multi-node cluster

# ELASTIC_NODE_NAME_1=elastdocker-node-1
# ELASTIC_NODE_NAME_2=elastdocker-node-2

#----------- External Data Sources --------------#
# Configure paths to external log sources
# Use absolute paths or Docker volume names

# FreqTrade logs path (if using FreqTrade pipeline)
# Default: ./freqtrade-logs (relative to docker-compose.yml)
FREQTRADE_LOGS_PATH=./freqtrade-logs

# Add other log sources as needed
# NGINX_LOGS_PATH=./nginx-logs
# APP_LOGS_PATH=./application-logs

#----------- Optional: Cloud Backup -------------#
# AWS S3 credentials for Elasticsearch snapshots
# Only needed if configuring automated backups to S3
# Leave commented if not using S3 snapshots

# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# S3_BUCKET_NAME=your-elasticsearch-backup-bucket
# S3_REGION=us-east-1

#----------- Optional: Monitoring ---------------#
# Grafana admin password (if using monitoring stack)
# Only needed with docker-compose.monitor.yml

# GRAFANA_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD

#----------- Optional: Alerting -----------------#
# Email alerting configuration (for ElastAlert2)
# Only needed if configuring email alerts

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=alerts@example.com
# SMTP_PASSWORD=CHANGE_ME_SMTP_PASSWORD
# ALERT_EMAIL=admin@example.com

#----------- Development Only -------------------#
# These settings should NOT be used in production
# Only for local development/testing

# ELASTIC_PASSWORD_DEV=changeme
# LOG_LEVEL=debug
